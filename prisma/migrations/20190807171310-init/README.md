# Migration `20190807171310-init`

This migration has been generated by Mario Bermudez Jr at 8/7/2019, 5:13:10 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Todo"("id" TEXT NOT NULL  ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,"title" TEXT NOT NULL DEFAULT '' ,"prioritize" BOOLEAN   ,"completed" BOOLEAN   ,"author" TEXT   REFERENCES "User"(id),PRIMARY KEY ("id"));

CREATE UNIQUE INDEX "lift"."Todo.id._UNIQUE" ON "Todo"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190624162221-init..20190807171310-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,20 +1,19 @@
 datasource db {
   provider = "sqlite"
-  url      = "file:dev.db"
-  default  = true
+  url      = "file:./dev.db"
 }
 generator photon {
   provider = "photonjs"
-  output   = "../node_modules/@generated/photon"
 }
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
   name  String?
   posts Post[]
+  todos Todo[]
 }
 model Post {
   id        String   @default(cuid()) @id @unique
@@ -23,5 +22,15 @@
   published Boolean
   title     String
   content   String?
   author    User?
+}
+
+model Todo {
+  id         String   @default(cuid()) @id @unique
+  createdAt  DateTime @default(now())
+  updatedAt  DateTime @updatedAt
+  title      String
+  prioritize Boolean? @default(false)
+  completed  Boolean? @default(false)
+  author     User?
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190807171310-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190807171310-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
