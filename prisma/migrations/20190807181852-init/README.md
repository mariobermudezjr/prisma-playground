# Migration `20190807181852-init`

This migration has been generated by Mario Bermudez Jr at 8/7/2019, 6:18:52 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."Post";
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190807171310-init..20190807181852-init
--- datamodel.dml
+++ datamodel.dml
@@ -10,22 +10,11 @@
 model User {
   id    String  @default(cuid()) @id @unique
   email String  @unique
   name  String?
-  posts Post[]
   todos Todo[]
 }
-model Post {
-  id        String   @default(cuid()) @id @unique
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  published Boolean
-  title     String
-  content   String?
-  author    User?
-}
-
 model Todo {
   id         String   @default(cuid()) @id @unique
   createdAt  DateTime @default(now())
   updatedAt  DateTime @updatedAt
```

## Photon Usage

You can use a specific Photon built for this migration (20190807181852-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190807181852-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
